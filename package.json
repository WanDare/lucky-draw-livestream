{
  "name": "lucky-draw-livestream",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "typescript": "~5.8.3",
    "vite": "^6.3.5",
    "vite-plugin-compression": "^0.5.1"
  },
  "dependencies": {
    "node-fetch": "^3.3.2",
    "phaser": "^3.90.0"
  }
}
